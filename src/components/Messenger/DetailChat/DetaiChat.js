import React from "react";
import { Paper } from "@material-ui/core";

const DetaiChat = () => {
  return (
    <Paper style={{ flex: 1, backgroundColor: "rgba(0, 0, 0,0.2)" }}>
      DetaiChat
    </Paper>
  );
};

export default DetaiChat;
